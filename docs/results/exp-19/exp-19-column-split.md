# EXP-19 メモ列（失敗理由）を一覧行に同居させるか別表に分けるか

| | |
| --- | --- |
| Experiment | EXP-19 / column-split |
| Starting SHA | `3f35d91961ef` (作業ツリーに未コミットの変更あり) |
| Meter version | `expkit/2` |
| Hypothesis (frozen before result) | 1) 同居のコストは『走査する行数 × 行の太さ』。1ページ(LIMIT 100)だけなら走査が少ないので差は小さい。 2) テナント全体を走査する集計では、太い行(メモ同居)の clustered index はページ数が多く、    狭い表より明確に遅い（メモを取らなくても）。 3) メモが要る画面では、分割版は『一覧＋id群のメモをバッチ取得』（DataLoader 相当）で足りる    （往復は増えるが1ページぶんだけ）。 |
| Environment | go1.24.7 linux/amd64 cpu=4 gomaxprocs=4 mysql=8.0.46-0ubuntu0.24.04.4 sha=3f35d91961ef+dirty |
| Started / Ended | 2026-09-07T02:50:58Z / 2026-09-07T02:52:50Z |

## Workload

- `detail_bytes` = 約3KB
- `page_limit` = 100
- `rows` = 200000

## Results

### 1ページ一覧(LIMIT 100・メモ取らない): 同居 vs 分割 — OK

| 測ったもの | 値 |
| --- | --- |
| inline_p50_ms | 1.055 |
| split_p50_ms | 0.774 |

- 同居 1.055689ms → 分割 774.75µs（走査が少ないので差は小さい）

### 全体走査の集計(COUNT・メモ取らない): 同居版（太い行） — **事故あり**

| 測ったもの | 値 |
| --- | --- |
| p50_ms | 967.327 |

### 全体走査の集計(COUNT・メモ取らない): 分割版（狭い表） — OK

| 測ったもの | 値 |
| --- | --- |
| p50_ms | 70.622 |

- 同居 967.327685ms → 分割 70.622654ms（約 13.7 倍。太い clustered index はページ数が多い）

### メモが要る画面: 同居版（1クエリでメモも取る） — OK

| 測ったもの | 値 |
| --- | --- |
| p50_ms | 1.206 |

### メモが要る画面: 分割版（一覧＋id群のメモをバッチ取得, DataLoader 相当） — OK

| 測ったもの | 値 |
| --- | --- |
| p50_ms | 2.408 |

- メモが要るときは分割版でも 2.408299ms で足りる（2クエリでも1ページぶんだけ）

## Verdict

失敗理由のような大きなメモを一覧行に同居させると、メモを取らない一覧でも行が太って遅くなる。メモを別表に分ければ一覧は狭い行だけを読むので速く、メモが要る画面では『一覧＋id群のメモをバッチ取得』（GraphQL の DataLoader 相当）で足りる。→ GraphQL でも、要求スカラーは投影し、メモ/理由は別表＋別リゾルバのバッチ取得にする。

## 適用範囲

- MySQL 8.0 / 20万行 / 1ページ100件 / メモ約3KB
- 一覧は status で絞って id 順に LIMIT。メモ列は VARCHAR(2000)
- 分割版の一覧は狭い表、メモは別表（同じ (tenant_id, id)）

## 保証しない範囲・未検証

- 絶対値はバッファプールに載った状態のもの。ディスクから読む状況では差が広がりうる
- メモのサイズ・1ページ件数で差は動く。TEXT/BLOB ではさらに差が出る（別ページ格納）
- 分割は書き込みが2表になる（一覧行とメモを両方入れる）。整合はトランザクションで取る

## 再利用できる成果物

- internal/splitlab: 同居/分割の一覧・詳細取得（DataLoader 相当のバッチ取得）

## 次の実験

- なし

