# EXP-16 担当テナント数（IN サイズ）の上限: 畳んだ1クエリはどこから重くなるか

| | |
| --- | --- |
| Experiment | EXP-16 / owned-tenant-limit |
| Starting SHA | `65f8b884a5ab` (作業ツリーに未コミットの変更あり) |
| Meter version | `expkit/2` |
| Hypothesis (frozen before result) | 1) 担当テナントを IN で畳んだ1クエリの所要は、IN の要素数（担当テナント数）とともに増える。 2) テナント 30 程度なら1クエリは十分速い（畳んで良い）。 3) IN が非常に大きい（数百〜千）と1クエリが重くなり、分けた方が速くなる領域が現れる。 |
| Environment | go1.24.7 linux/amd64 cpu=4 gomaxprocs=4 mysql=8.0.46-0ubuntu0.24.04.4 sha=65f8b884a5ab+dirty |
| Started / Ended | 2026-09-06T23:54:36Z / 2026-09-06T23:54:38Z |

## Workload

- `max_tenants` = 1000
- `samples_per_size` = 50

## Results

### IN サイズ = 10 担当テナント — OK

| 数えたもの | 値 |
| --- | --- |
| rows | 10 |

| 測ったもの | 値 |
| --- | --- |
| p50_ms | 0.440 |
| p95_ms | 0.708 |
| p99_ms | 0.752 |

### IN サイズ = 30 担当テナント — OK

| 数えたもの | 値 |
| --- | --- |
| rows | 30 |

| 測ったもの | 値 |
| --- | --- |
| p50_ms | 0.787 |
| p95_ms | 0.868 |
| p99_ms | 0.874 |

### IN サイズ = 100 担当テナント — OK

| 数えたもの | 値 |
| --- | --- |
| rows | 100 |

| 測ったもの | 値 |
| --- | --- |
| p50_ms | 2.325 |
| p95_ms | 2.524 |
| p99_ms | 2.922 |

### IN サイズ = 300 担当テナント — OK

| 数えたもの | 値 |
| --- | --- |
| rows | 300 |

| 測ったもの | 値 |
| --- | --- |
| p50_ms | 4.458 |
| p95_ms | 4.780 |
| p99_ms | 5.433 |

### IN サイズ = 1000 担当テナント — OK

| 数えたもの | 値 |
| --- | --- |
| rows | 500 |

| 測ったもの | 値 |
| --- | --- |
| p50_ms | 15.146 |
| p95_ms | 16.594 |
| p99_ms | 18.314 |

### 畳む vs 分ける（300 テナント） — OK

| 測ったもの | 値 |
| --- | --- |
| one_query_in300_p50_ms | 4.654 |
| three_queries_in100_合計_p50_ms | 6.453 |

- IN(300) 1クエリ p50=4.654378ms / IN(100)×3 合計 p50=6.453373ms
- 分けると往復が3回になる。IN が重くなる前は1クエリが有利、重くなると分割が効く

## Verdict

担当テナントを IN で畳んだ1クエリの所要は IN サイズとともに増える。テナント 30 程度なら1クエリで十分速い（畳んで良い）。IN が非常に大きくなると、コンテナあたりの担当テナント数に上限を設けて分割する方が良い領域が出る。担当テナント数の上限は、この所要と接続予算（poolbudget）の両方から決める。

## 適用範囲

- MySQL 8.0 / cmd_command の poll 索引 (tenant_id,state,scheduled_for) / 各サイズ 50 サンプル
- 1テナント1命令（due 済み）。IN の要素数だけを変えて1クエリの所要を測る

## 保証しない範囲・未検証

- 最適な上限は行数・索引・DB の忙しさで動く。この数字はこのホストのもの
- IN が数千を超えるとプランナやパケットサイズの別の効き方が出る（未測定）
- 接続予算（poolbudget）との兼ね合いで、担当テナント数はコンテナ数とも連動する

## 再利用できる成果物

- internal/fanoutlab: MeasureFoldQuery（IN サイズごとの1クエリ所要）

## 次の実験

- なし

