# EXP-47 重複・逆順のイベントでも版で単調適用すれば最新に収束する

| | |
| --- | --- |
| Experiment | EXP-47 / ordered-idempotent-consume |
| Starting SHA | `174e88c41208` (作業ツリーに未コミットの変更あり) |
| Meter version | `expkit/2` |
| Hypothesis (frozen before result) | 1) 版で単調適用（incoming > last のときだけ適用）なら、重複・逆順が来ても各キーは最大版に収束。 2) 素朴な last-write-wins（到着順に上書き）は、古い版が最後に来ると古い値に退行する。 3) 重複（同じ版）は単調適用では二度目が無視される（冪等）。 |
| Environment | go1.26.0 linux/amd64 cpu=4 gomaxprocs=4 mysql= sha=174e88c41208+dirty |
| Started / Ended | 2026-09-07T12:11:06Z / 2026-09-07T12:11:06Z |

## Results

### 版で単調適用: 重複・逆順でも最新に収束（退行なし） — OK

| 数えたもの | 値 |
| --- | --- |
| applied | 3 |
| ignored | 3 |
| r1 | 3 |
| r2 | 6 |
| wrong_keys | 0 |

- 最終 r1=3 r2=6（想定 r1=3,r2=6）。重複/逆順は無視 3 回

### 素朴 last-write-wins: 古い版が最後に来て退行 — **事故あり**

| 数えたもの | 値 |
| --- | --- |
| r1 | 1 |
| r2 | 5 |
| wrong_keys | 2 |

- 最終 r1=1（版1が最後に到着し退行）・想定 3。誤り 2 キー

## Verdict

イベントは重複・逆順で届きうる。キーごとに版を持ち『今より新しい版だけ適用』する単調消費にすれば、重複・逆順が来ても最新版に収束し退行しない（冪等）。素朴な到着順上書きは、古い版が最後に来ると退行する。差分でなく版つきの最新値/スナップショットを配ると更に堅い。

## 適用範囲

- 純 Go（DB 不要）/ 配送順 [r1:2, r2:6, r1:3, r1:3(dup), r2:5, r1:1] / 想定最終 r1=3,r2=6
- 単調適用 = incoming version > last のときだけ適用（それ以外は無視＝冪等・逆順耐性）
- 版は fence（EXP-30）・SSE の版配信（EXP-38/41）と同じ考え

## 保証しない範囲・未検証

- 版はキー単位に単調増加する前提（サーバが採番）。版が無い/回る場合は別途（時刻＋タイブレーク等）
- 差分でなく『版つきスナップショット/最新値』を配る設計だと、逆順・欠落に強い（落としても次で収束）
- 部分更新（フィールドごとに版）が要るなら、フィールド単位の版か CRDT を検討（本実験外）

## 再利用できる成果物

- docs/event-ordering.md: 順序・重複に強い冪等消費（版で単調適用）

## 次の実験

- EXP-48 保持期間の運用

